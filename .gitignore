# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binaries
*.test
*.out

# Go workspace file
go.work
go.work.sum

# Vendor directory
vendor/

# Build outputs
pb/
server_bin
client_bin

# Optional: Go module cache
# Uncomment below to ignore the Go module cache
# .go/pkg/ 

# vemu_service build artefacts
vemu_service/cgo_bridge/*.o
vemu_service/cgo_bridge/libvemu.a
# 通配全部 vemu_service/src 下生成的 .o（包括子目录）
vemu_service/src/**/*.o

*_run.txt

# vemu_service test executables
vemu_service/cgo_bridge/test_decoder*

# trap 测试可执行
vemu_service/cgo_bridge/test_trap

vemu_service/cgo_bridge/test_mem